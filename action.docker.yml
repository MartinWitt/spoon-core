
name: 'Spoon-Rebuilder'
description: 'Greet someone'
inputs:
  token:
    description: 'GitHub token'
    required: true
runs:
  using: 'docker'
  image: 'docker://ghcr.io/martinwitt/infer-train:dev'
  #pre-entrypoint: '/work/application'
  #post-entrypoint: '/work/application'